# Nixpacks only: Railway’s default builder is Railpack (see railpack.json).
# libgomp1: Paddle; libgl1: OpenCV (cv2) via PaddleOCR needs libGL.so.1
[phases.setup]
aptPkgs = ["...", "libgomp1", "libgl1"]
